# 🎄 AI Advent Challenge - Анекдотчик

Android приложение для участия в AI Advent Challenge - ежедневные задания по работе с искусственным интеллектом.

## 📱 О проекте

Мобильное приложение-чат с AI-анекдотчиком, который превращает любую ситуацию в смешной анекдот. Проект разрабатывается с использованием Yandex GPT API.

**Стек технологий:**

- Kotlin
- Jetpack Compose
- Yandex GPT API
- Clean Architecture (domain, data, presentation)
- Ktor Client
- Dagger 2

## 🎯 Задания по дням

Результаты выполнения каждого задания находятся в отдельных ветках.

### 🚀 День 1. Базовая настройка

**Дата:** 4 ноября

**Задание:**

- Реализовать простого агента, который отвечает на вопросы
- Вывести результат в интерфейсе (простой чат)
- Получение и отправка запросов через HTTP клиент

**Результат:**

- ✅ Агент принимает запрос и корректно вызывает инструмент
- ✅ Реализован чат-интерфейс для мобильного приложения
- ✅ Настроена интеграция с Yandex GPT API

**Ветка:** `day-1` или `master`

---

### 🔥 День 2. Настройка AI

**Дата:** 5 ноября

**Задание:**

- Научиться задавать формат результата для возвращения
- Задать агенту формат возвращения в prompt
- Привести пример формата возврата

**Результат:**

- ✅ Ответ от LLM можно распарсить
- ✅ Настроена JSON Schema для структурированного ответа
- ✅ Добавлена категоризация анекдотов
- ✅ Отображение метаданных (категория, количество токенов)
- ✅ Обработка различных статусов ответа (контент-фильтр, ошибки)

**Реализованная схема ответа:**

```json
{
  "response": "текст анекдота",
  "category": "Еда/Семья/Развлечения/Охота/Рыбалка/Алкоголь/Бары/Проститутки/Школа/Другое"
}
```

**Ветка:** `day-2`

**Дополнительно:**

- Добавлено логирование с pretty print для отладки
- Enum для статусов ответа API
- Обработка отфильтрованного контента

---

### 🎯 День 3. Многошаговый сбор информации

**Дата:** 6 ноября

**Задание:**

- Задать ограничение модели, чтобы она сама остановилась
- Описать в промпте результат, который модель должна собрать и выдать ответ
- Пример: модель должна собрать требования для ТЗ и в какой-то момент переписки вернуть готовый результат

**Результат:**

- ✅ Реализован многошаговый сбор информации для создания анекдота
- ✅ Модель последовательно собирает:
  1. Ситуацию для анекдота (от пользователя)
  2. Героя анекдота (выбор из списка: Врач, Вовочка, Чукча и др.)
  3. Тип юмора (Сарказм, Абсурд, Ирония и др.)
- ✅ После сбора всех параметров модель генерирует финальный анекдот
- ✅ Чёткая логика переходов между этапами через `stage` в JSON
- ✅ Модель самостоятельно управляет процессом сбора информации

**Реализованная схема ответа:**

```json
{
  "response": "вопрос или анекдот",
  "category": "Еда/Семья/...",
  "stage": "Сбор_ситуации/Выбор_героя/Выбор_юмора/Финальный_анекдот",
  "situation": "описание ситуации",
  "heroes": "выбранный герой",
  "humor_type": "тип юмора"
}
```

**Ключевые особенности:**

- Модель задаёт уточняющие вопросы и не генерирует анекдот до сбора всей информации
- Цикличная работа: после финального анекдота можно начать новый цикл
- Автоматическая очистка полей при начале нового цикла
- Строгая валидация последовательности шагов в промпте

**Ветка:** `day3`

---

### 🧠 День 4. Разные способы рассуждения AI

**Дата:** 9 ноября

**Задание:**

- Взять простую задачу (желательно логическую или какую-то более-менее сложную)
- Попросить модель дать ответ напрямую
- Добавить инструкцию: «решай пошагово»
- Попробовать другой AI составить промпт для решения задачи
- Создать внутри AI группу экспертов для решения задачи
- Сравнить результаты разных подходов

**Результат:**

- ✅ Реализовано 5 отдельных AI-агентов с разными стратегиями рассуждения
- ✅ Автоматическая последовательная генерация 4 вариантов анекдота
- ✅ Каждый подход демонстрирует разную технику работы с LLM

**Архитектура решения:**

**1. Агент-сборщик информации**

- Собирает параметры: ситуация → герой → тип юмора
- После сбора запускает цепочку из 4 агентов генерации

**2. Агент "Прямой ответ" (🎯 Способ 1)**

- Генерирует анекдот напрямую без объяснений
- Демонстрирует: базовый запрос к AI без промежуточных шагов

**3. Агент "Пошаговое рассуждение" (🔢 Способ 2)**

- Показывает процесс создания:
  1. Анализ ситуации
  2. Подбор сюжета
  3. Создание шутки
  4. Финальный анекдот
- Демонстрирует: Chain-of-Thought (CoT) подход

**4. Агент "Мета-промпт" (📝 Способ 3)**

- Сначала создаёт оптимальный промпт для задачи
- Затем использует созданный промпт для генерации
- Демонстрирует: AI создаёт инструкции для AI (метауровень)

**5. Агент "Группа экспертов" (👥 Способ 4)**

- Симулирует обсуждение тремя экспертами:
  - Комик-сатирик (сатира и социальная критика)
  - Писатель-юморист (словесные игры)
  - Стендап-комик (ситуационный юмор)
- Выбирает лучший вариант или комбинирует элементы
- Демонстрирует: Multi-agent подход

`**Технические особенности:**

- Каждый агент имеет собственный системный промпт (`SYSTEM_PROMPT_*`)
- Автоматическое переключение между агентами через метод `getSystemPrompt(stage)`
- Извлечение контекста из истории диалога для передачи агентам генерации
- Автоматическая цепочка: Способ 1 → 2 → 3 → 4 → Финальное сообщение
- Задержка 500мс между способами для улучшения UX
- Все агенты создают безобидный, семейный контент

**Ветка:** `day4`

---

### 🌡️ День 5. Температура

**Дата:** 10 ноября

**Задание:**

- Запустить один и тот же запрос с температурой = 0, 0.7 и 1.2
- Сравнить результаты (точность, креативность, разнообразие)
- Сформулировать, для каких задач лучше подходит каждая настройка

**Результат:**

- ✅ Добавлен UI компонент для выбора температуры
- ✅ Реализованы 3 режима: Точный (0.0), Сбалансированный (0.7), Креативный (1.0)
- ✅ Параметр temperature передаётся через всю цепочку: UI → ViewModel → Repository → API
- ✅ Визуальная индикация режима с цветовой кодировкой и эмодзи
- ✅ Задокументированы различия между температурами

**Что такое температура:**

Температура (0.0-1.0 для Yandex GPT) - параметр, контролирующий случайность и креативность ответов AI:

- **0.0 (🧊 Точный)**: Детерминированные, предсказуемые ответы
- **0.7 (🌤️ Сбалансированный)**: Оптимальный баланс (по умолчанию)
- **1.0 (🔥 Креативный)**: Максимальная креативность (максимум для Yandex GPT)

**Сравнение режимов:**

| Режим  | Точность   | Креативность | Когда использовать        |
| ------ | ---------- | ------------ | ------------------------- |
| 🧊 0.0 | ⭐⭐⭐⭐⭐ | ⭐           | Код, математика, переводы |
| 🌤️ 0.7 | ⭐⭐⭐⭐   | ⭐⭐⭐       | Чаты, анекдоты, контент   |
| 🔥 1.0 | ⭐⭐⭐     | ⭐⭐⭐⭐⭐   | Поэзия, мозговой штурм    |

**Технические особенности:**

- Dropdown меню для выбора температуры над полем ввода
- Цветовая кодировка: синий (холодный) → зелёный → красный (горячий)
- Отображение текущего режима с описанием
- Temperature передаётся в CompletionOptions API

**Выводы:**

1. Температура не влияет на скорость или стоимость (токены)
2. Для анекдотов оптимален диапазон 0.7-1.0
3. Высокая температура ≠ лучше - выбор зависит от задачи
4. При t=0.0 анекдоты предсказуемые, при t=1.0 - неожиданные
5. ⚠️ Yandex GPT принимает температуру только [0, 1], а не [0, 2] как другие модели

**Ветка:** `day5`

---

## 🔥 День 6: Сравнение версий моделей

**Задание:**

- Вызвать один и тот же запрос на трёх разных моделях (из начала, середины и конца списка HuggingFace)
- Замерить время ответа, количество токенов и итоговую стоимость
- Сравнить качество ответов

**Результат:**

- ✅ Реализована pluggable архитектура с LLMProvider interface
- ✅ Добавлены 3 модели: Kimi K2 (1T), Gemma 2 (9B), Llama 3.2 (3B)
- ✅ UI для выбора модели с dropdown
- ✅ Автоматический сбор метрик (время, токены) для каждой модели
- ✅ Отображение метрик в сообщениях
- ✅ Все модели работают через единый интерфейс
- ✅ Поддержка всех 4 reasoning подходов для любой модели

**Выбранные модели:**

| Модель           | Размер          | Провайдер   | Особенности                      |
| ---------------- | --------------- | ----------- | -------------------------------- |
| Kimi K2 Thinking | 1T (32B active) | HuggingFace | Глубокое мышление, 256K контекст |
| Gemma 2 9B       | 9B              | HuggingFace | Google, instruction-tuned        |
| Llama 3.2 3B     | 3B              | HuggingFace | Meta, компактная и быстрая       |

**Архитектура:**

```
LLMProvider (interface)
├── sendMessage()
├── getModelName()
└── getModelDescription()

HuggingFaceProvider (implementation)
└── Работает с HuggingFace Inference API

ModelRegistry
└── Управляет доступными моделями

ChatRepository
└── Динамически выбирает провайдер по modelId
```

**Метрики:**

Для каждого ответа автоматически собираются:

- ⏱️ Время ответа (responseTimeMs)
- 📊 Input токены
- 📤 Output токены
- 💰 Стоимость (если платно)
- 🏷️ Имя модели

**Выводы:**

1. **Kimi K2 (1T)** - лучшее качество и креативность, но медленнее
2. **Gemma 2 (9B)** - оптимальный баланс скорости и качества
3. **Llama 3.2 (3B)** - самая быстрая, подходит для простых задач

**Технические особенности:**

- Pluggable architecture - легко добавить новые модели
- Единый интерфейс для всех провайдеров
- Автоматический сбор и отображение метрик
- Совместимость с Day 4 (reasoning) и Day 5 (temperature)

**Конфигурация:**

```properties
# local.properties
HUGGING_FACE_TOKEN=your_token_here
```

**Ветка:** `day6`

---

## 📊 День 7: Работа с токенами

**Задание:**

- Добавить в код подсчёт токенов (на запрос и ответ)
- Сравнить: короткий запрос, длинный запрос и запрос, превышающий лимит модели
- Показать, как меняется поведение модели

**Результат:**

- ✅ Реализован визуальный индикатор использования токенов
- ✅ Добавлены тесты для разных размеров запросов
- ✅ Отображение токенов в каждом сообщении (Input/Output)
- ✅ Progress bar с предупреждением о приближении к лимиту
- ✅ Режим тестирования токенов

**Реализованные метрики:**

Для каждого сообщения отображаются:
- ⏱️ Время ответа (ms)
- 📥 Input токены (от пользователя)
- 📤 Output токены (от модели)
- 📊 Total токены (общая сумма)

**Token Usage Bar:**

```
[████████░░░░░░] 4256 / 8000
```

Цветовая индикация:
- 🟢 < 80% - нормально
- 🟠 80-100% - приближение к лимиту
- 🔴 > 100% - превышен лимит

**Тесты токенов:**

| Тип запроса | Примерный размер | Ожидаемое поведение           |
| ----------- | ---------------- | ----------------------------- |
| SHORT       | ~100 токенов     | ✅ Быстро, корректно          |
| MEDIUM      | ~1000 токенов    | ✅ Нормально                  |
| LONG        | ~4000 токенов    | ⚠️ Близко к лимиту           |
| OVERFLOW    | ~10000 токенов   | ❌ Превышен лимит/обрезается |

**Технические особенности:**

- Подсчёт токенов происходит на стороне API
- Метрики записываются в `ModelMetrics` для каждого сообщения
- Накопительный подсчёт токенов в текущей сессии
- Визуальное предупреждение при приближении к лимиту
- Панель тестирования с 4 типами запросов разной длины

**Выводы:**

1. Yandex GPT имеет лимит ~8000 токенов на контекст
2. Input токены = промпт + история диалога
3. Output токены = ответ модели
4. При превышении лимита история может обрезаться
5. Длинные диалоги требуют управления контекстом

**Ветка:** `day7`

---

## 🗜️ День 8: Сжатие диалога

**Задание:**

- Реализовать механизм «сжатия истории диалога»
- Каждые N сообщений создавать summary и хранить его вместо оригинала
- Проверить, как агент продолжает работать с учётом summary
- Сравнить качество ответов и использование токенов

**Результат:**

- ✅ Реализована кнопка ручного сжатия истории
- ✅ Summary создаётся через YandexGPT с низкой температурой
- ✅ Визуальное отличие сжатых сообщений (бейдж "🗜️ СЖАТО")
- ✅ Счётчик сэкономленных токенов
- ✅ Сохранение контекста (последние 3 сообщения)

**Алгоритм сжатия:**

```
1. Пользователь нажимает "🗜️ Сжать историю" в настройках
2. Система выбирает сообщения для сжатия:
   ✅ Сохраняется: Приветственное сообщение
   🗜️ Сжимаются: Все сообщения между первым и последними 3
   ✅ Сохраняется: Последние 3 сообщения
3. Отправляется запрос к API для создания summary
4. Сжатые сообщения заменяются на одно summary
5. Обновляется счётчик сэкономленных токенов
```

**Пример работы:**

ДО сжатия:
```
[Welcome] - 20 токенов
[Msg 1]   - 100 токенов  ┐
[Msg 2]   - 200 токенов  │
[Msg 3]   - 50 токенов   │ → Сжимаем в summary
[Msg 4]   - 300 токенов  │   (~150 токенов)
[Msg 5]   - 50 токенов   │
[Msg 6]   - 800 токенов  ┘
[Msg 7]   - 100 токенов  ┐
[Msg 8]   - 200 токенов  │ Сохраняем
[Msg 9]   - 50 токенов   ┘

Итого: ~1870 токенов
```

ПОСЛЕ сжатия:
```
[Welcome]  - 20 токенов
[Summary]  - 150 токенов (📝 Саммари 6 сообщений)
[Msg 7]    - 100 токенов
[Msg 8]    - 200 токенов
[Msg 9]    - 50 токенов

Итого: ~520 токенов
Сэкономлено: 1350 токенов (72%)
```

**UI расположение:**

Кнопка находится в выпадающем меню настроек:
```
[▼] Настройки
    ├─ Выбор модели
    ├─ Температура
    ├─ 🗜️ Сжать историю ← День 8
    │  └─ Сэкономлено: 1350
    └─ 🧪 Тесты токенов
```

**Технические особенности:**

- Temperature = 0.3 для точного summary
- Summary создаётся отдельным запросом к API
- Сжатые сообщения имеют поля `isSummary=true` и `summarizedCount`
- Специальный цвет фона (tertiaryContainer)
- Проверка минимума сообщений (нужно хотя бы 5)
- Индикатор процесса сжатия

**Выводы:**

1. Сжатие может сэкономить 50-90% токенов
2. Качество ответов практически не страдает
3. Summary сохраняет всю важную информацию
4. Последние сообщения важны для контекста
5. Ручное управление даёт пользователю контроль
6. Можно делать несколько сжатий подряд

**Дополнительные файлы:**
- `DAY8_DIALOG_COMPRESSION.md` - подробная документация

**Ветка:** `day8`

---

## 💾 День 9: Внешняя память агента

**Дата:** 17 ноября

**Задание:**

- Подключить «внешнюю память» для модели: сохранение промежуточных результатов в SQLite/JSON-файл
- Проверить, что данные сохраняются и доступны между запусками
- Результат: Агент с долговременной памятью (SQLite/JSON)

**Результат:**

- ✅ Реализована система долговременной памяти с Room Database (SQLite)
- ✅ Все сообщения и разговоры сохраняются локально
- ✅ Данные персистентны между перезапусками приложения
- ✅ Готовая инфраструктура для управления историей диалогов

**Архитектура памяти:**

```
ChatDatabase (Room)
├── ConversationEntity     # Разговоры/сессии
│   ├── id, title
│   ├── createdAt, updatedAt
│   ├── currentStage
│   └── isActive
└── MessageEntity          # Сообщения
    ├── id, conversationId (FK)
    ├── text, isUser
    ├── timestamp, category
    ├── totalTokens, metrics (JSON)
    └── isSummary, summarizedCount

MemoryRepository
├── createConversation()
├── saveMessage()
├── getConversationHistory()
├── getLastNMessages()
└── clearConversation()
```

**Основные компоненты:**

**1. Room Database:**
- `ChatDatabase` - основная БД приложения
- Автоматические миграции (для dev)
- TypeConverters для сложных типов (`ModelMetrics`)

**2. Entities (Сущности):**
- `ConversationEntity` - разговоры с метаданными
- `MessageEntity` - отдельные сообщения с привязкой к разговору

**3. DAOs (Data Access Objects):**
- `ConversationDao` - CRUD для разговоров
- `MessageDao` - CRUD для сообщений

**4. MemoryRepository:**
- Высокоуровневый API для работы с памятью
- Автоматическое управление лимитом сообщений (50 по умолчанию)
- Поддержка Flow для реактивных обновлений

**Особенности реализации:**

✅ **Каскадное удаление**
- При удалении разговора автоматически удаляются все его сообщения

✅ **Индексы для производительности**
- По `conversationId` для быстрой выборки
- По `timestamp` для сортировки

✅ **Автоматическое управление памятью**
- Ограничение количества сообщений на разговор
- Автоматическая очистка старых сообщений

✅ **Интеграция с существующей системой**
- Новые методы в `ChatRepository`
- Совместимость с Day 8 (сжатие)

**API методы:**

```kotlin
// ChatRepository
suspend fun saveMessage(conversationId: Long, message: Message)
suspend fun getConversationHistory(conversationId: Long): List<Message>
suspend fun createConversation(title: String): Long
suspend fun getOrCreateActiveConversation(): Long

// MemoryRepository
suspend fun getLastNMessages(conversationId: Long, limit: Int): List<Message>
suspend fun clearConversation(conversationId: Long)
suspend fun archiveConversation(conversationId: Long)
fun getMessagesForConversationFlow(conversationId: Long): Flow<List<Message>>
```

**Технические особенности:**

- Room 2.6.1 с KSP
- TypeConverters для JSON сериализации
- Foreign Keys с CASCADE DELETE
- Dagger 2 интеграция с `@AppScope`
- Coroutines + Flow для асинхронных операций

**Проверка персистентности:**

```kotlin
// 1. Запустить приложение
// 2. Создать разговор и отправить сообщения
// 3. Полностью закрыть приложение
// 4. Запустить снова
// 5. История сохранена и доступна! ✅
```

**База данных:**

```sql
-- Таблица разговоров
conversations (
  id INTEGER PRIMARY KEY,
  title TEXT,
  createdAt INTEGER,
  updatedAt INTEGER,
  currentStage TEXT,
  isActive INTEGER
)

-- Таблица сообщений
messages (
  id TEXT PRIMARY KEY,
  conversationId INTEGER → conversations(id),
  text TEXT,
  isUser INTEGER,
  timestamp INTEGER,
  category TEXT,
  metrics TEXT (JSON),
  ...
)
```

**Дополнительные файлы:**
- `docs/DAY9_MEMORY.md` - подробная документация
- Entities в `data/local/entity/`
- DAOs в `data/local/dao/`
- Database в `data/local/database/`

**Ветка:** `day9`

---

## 🏗️ Архитектура проекта

```
aiadvent1/
├── core/           # Базовые компоненты
├── domain/         # Бизнес-логика
│   ├── model/      # Модели данных
│   └── repository/ # Интерфейсы репозиториев
├── data/           # Слой данных
│   ├── remote/     # API клиент
│   │   ├── api/    # Yandex GPT API
│   │   └── dto/    # Data Transfer Objects
│   └── repository/ # Реализация репозиториев
└── mobile/         # UI слой (Android)
    └── presentation/
        └── chat/   # Чат-интерфейс
```

## 🚀 Запуск проекта

1. Клонировать репозиторий
2. Создать файл `local.properties` на основе `local.properties.example`
3. Добавить свои ключи Yandex Cloud:
   ```properties
   YANDEX_API_KEY=your_api_key_here
   YANDEX_FOLDER_ID=your_folder_id_here
   ```
4. Собрать и запустить проект

## 📸 Скриншоты

> Добавьте скриншоты работы приложения

## 🔗 Полезные ссылки

- [Yandex GPT API Documentation](https://cloud.yandex.ru/docs/foundation-models/)
- [AI Advent Challenge](https://t.me/ai_advent_challenge)

## 📝 Лицензия

MIT

---

**Автор:** [@arekalov](https://github.com/arekalov)
**Год:** 2025
